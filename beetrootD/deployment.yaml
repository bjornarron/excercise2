apiVersion: apps/v1
kind: Deployment
metadata:
  name: beetroot-deployment
  labels:
    app: beetroot
spec:
  containers:
    - name: beetroot
      image: https://github.com/bjornarron/excercise2/Dockerfile
      ports:
      - port: 80
        targetPort: 80
        nodePort: 30007